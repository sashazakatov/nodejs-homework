const registerScheme =  require('./registerScheme');
const subscriptionScheme = require('./subscriptionScheme');
const emailScheme = require('./emailScheme');

module.exports = {
    registerScheme,
    subscriptionScheme,
    emailScheme,
}